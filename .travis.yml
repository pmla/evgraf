jobs:
  include:
    - language: python
      python:
        - "3.5"
        - "3.6"      # current default Python on Travis CI
      # command to install dependencies
      install:
        - pip install git+https://github.com/pmla/evgraf@TRAVIS_BRANCH
      # command to run tests
      script:
        - pytest tests/*
